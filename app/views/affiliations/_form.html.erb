<%= form_for(@affiliation) do |f| %>
  <% if @affiliation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@affiliation.errors.count, "error") %> prohibited this affiliation from being saved:</h2>

      <ul>
      <% @affiliation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_pre_id %><br />
    <%= f.text_field :first_pre_id %>
  </div>
  <div class="field">
    <%= f.label :second_pre_id %><br />
    <%= f.text_field :second_pre_id %>
  </div>
  <div class="field">
    <%= f.label :relationship %><br />
    <%= f.text_field :relationship %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
