<h1>Listing affiliations</h1>

<table>
  <tr>
    <th>First pre</th>
    <th>Second pre</th>
    <th>Relationship</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @affiliations.each do |affiliation| %>
  <tr>
    <td><%= affiliation.first_pre_id %></td>
    <td><%= affiliation.second_pre_id %></td>
    <td><%= affiliation.relationship %></td>
    <td><%= link_to 'Show', affiliation %></td>
    <td><%= link_to 'Edit', edit_affiliation_path(affiliation) %></td>
    <td><%= link_to 'Destroy', affiliation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Affiliation', new_affiliation_path %>
